import ProductsList from "..";
import { renderToHTML } from "next/dist/server/render";

describe("Product", () => {
  it("Hi test", () => {
    expect(true).toBe(true);
  });
  it("Not hi test", () => {
    expect(true).toBe(true);
  });
});
